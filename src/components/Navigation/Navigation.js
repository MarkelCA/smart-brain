const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: "flex-end", padding: '.5em' }}>
            <p>Sign out</p>

        </nav>

    )
}

export default Navigation
